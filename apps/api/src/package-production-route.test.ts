import Fastify from "fastify";
import { describe, expect, it } from "vitest";
import { registerPackageProductionRoute } from "./package-production-route.js";
const body = { day: { date: "2026-08-30", timezone: "America/Sao_Paulo" }, packageVersion: "1.0.0", generatedAt: "2026-08-30T12:00:00-03:00", checksum: "sha256:example", sourceEvidence: [{ sourceId: "fixture", contentHash: "hash", collectedAt: "2026-08-30T12:00:00-03:00", parserVersion: "1", decisionRule: "fixture" }], celebration: { title: "Celebracao", season: "Tempo Comum", color: "Verde", cycleYear: "B", celebrationRank: "Domingo", readings: [{ type: "GOSPEL", citation: "Referencia" }], massFlow: [{ section: "introductory-rites", note: "Nota" }], selectedSongs: [], sourceReference: { booklet: "Livreto" } }, validationEvidence: { gospelMatches: true, titleMatches: true, celebrationMatches: true, cycleMatches: true, precedenceMatches: true, referencesMatch: true } };
describe("package production route", () => { it("requires private authentication and derives the approval decision", async () => { const app = Fastify(); const inputs: unknown[] = []; registerPackageProductionRoute(app, { enqueue: async (input) => { inputs.push(input); return { id: "job-1" }; } }, "test-token", { write: async () => "audit.json" }); expect((await app.inject({ method: "POST", url: "/v1/internal/packages", payload: body })).statusCode).toBe(401); const response = await app.inject({ method: "POST", url: "/v1/internal/packages", headers: { authorization: "Bearer test-token" }, payload: body }); expect(response.statusCode).toBe(202); expect(response.json()).toEqual({ jobId: "job-1", decision: "APPROVED" }); expect(inputs).toHaveLength(1); await app.close(); }); });
